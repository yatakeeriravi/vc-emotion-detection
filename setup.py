from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Ml project to predict emotion from text',
    author='ravi',
    license='',
)
